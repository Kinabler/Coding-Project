<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create User</title>
    <link rel="stylesheet" href="./css/style.css">
    <!-- Add google fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Sansation:ital,wght@0,300;0,400;0,700;1,300;1,400;1,700&display=swap"
        rel="stylesheet">
</head>

<body class="sansation-light">
    <div class="Navbar">
        <ul>
            <li><a href="/">Home</a></li>
            <li><a class="active" href="/create-user">Create</a></li>
            <li><a href="/upload">Upload File</a></li>
            <li><a href="/about">About</a></li>
        </ul>
    </div>
    <div class="header page">
        <h1>Create User Form: </h1><br><br>
        <div class="fieldset-form">
            <form action="/create" method="POST">
                <fieldset>
                    <legend>User:</legend>
                    <label for="email">Email:</label>
                    <input type="email" id="email" name="email"><br><br>
                    <label for="username">Username:</label>
                    <input type="text" id="username" name="username"><br><br>
                    <label for="password">Password:</label>
                    <input type="password" id="password" name="password"><br><br>
                    <div class="status-fieldset <%= typeof message !== 'undefined' && message ? 
    (message.includes('Successfully') ? 'status-success' : 
    (message.includes('Failed') || message.includes('exists') ? 'status-error' : 'status-info')) : '' %>">
                        <% if (typeof message !=='undefined' && message) { %>
                            <p>
                                <%= message %>
                            </p>
                            <% } %>
                    </div>
                    <input type="submit" value="Submit">
                </fieldset>
            </form>

        </div>
</body>

</html>